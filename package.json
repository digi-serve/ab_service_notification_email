{
  "name": "notification_email",
  "version": "0.0.0",
  "description": "A service for sending smtp emails.",
  "main": "app.js",
  "author": "Johnny Hausman",
  "license": "MIT",
  "dependencies": {
    "ab-utils": "Hiro-Nakamura/ab-utils#master",
    "cote": "^0.21.1",
    "nodemailer": "^6.4.2",
    "nodemon": "^1.18.10"
  },
  "scripts": {
    "dev": "node_modules/.bin/nodemon --inspect=0.0.0.0:9229 app.js",
    "devBuild": "docker run -it --mount type=bind,source=\"$(pwd)\",target=/app -w /app node npm install",
    "devInstall": "docker run -it --mount type=bind,source=\"$(pwd)\",target=/app -w /app node bash"
  }
}
